# Simple elevator model in TLA+ 

This repo contains a very simple model of an elevator in TLA+.

It is intended as an example of how weak and strong fairness can be used to
express liveness properties.